version: "3.8"
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app  # optional: mount for live reload during dev
    environment:
      - FLASK_ENV=development
